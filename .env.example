# The current static playbook generator does not read environment variables.
# It builds deterministic Markdown entirely in the browser.

# If a future server-side AI integration is added, keep provider credentials
# outside the Vite client bundle. Never prefix secret keys with VITE_.
